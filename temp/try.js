
let obj = [{id: 1, name: "Koosta arve"}, {id: 2, name:'test asutus'}, {id:3, name:'nimi'}];
let dist = 1;
//let index = obj.findIndex(row => row.id === 1);
let objs = obj.slice();
let removed = obj.shift();
let pl1 =
console.log('indx', objs, removed, (dist +=1), (dist =+ 1));

