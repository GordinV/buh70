<?xml version = "1.0" encoding="Windows-1252" standalone="yes"?>
<VFPData>
  <grid>
    <sql>select id, coalesce(kood,'') as kood, coalesce(nimetus,'') as nimetus,  $2::integer as userId, dok
            from libs.nomenklatuur n
            where (n.rekvId = $1 or n.rekvid is null) and n.status &lt;&gt; 3</sql>
    <alias>curNomenklatuur</alias>
  </grid>
  <select>
    <sql>select n.*, $2::integer as userid, 'NOMENCLATURE' as doc_type_id,
                v.valuuta, v.kuurs,
                (n.properties::jsonb -&gt;&gt;'vat')::text as vat,
                (n.properties::jsonb -&gt;&gt;'konto')::text as konto,
                (n.properties::jsonb -&gt;&gt;'projekt')::text as projekt,
                (n.properties::jsonb -&gt;&gt;'tunnus')::text as tunnus,
                (n.properties::jsonb -&gt;&gt;'tegev')::text as tegev,
                (n.properties::jsonb -&gt;&gt;'allikas')::text as allikas,
                (n.properties::jsonb -&gt;&gt;'rahavoog')::text as rahavoog,
                (n.properties::jsonb -&gt;&gt;'artikkel')::text as artikkel                
                from libs.nomenklatuur n 
                left outer join docs.dokvaluuta1 v on v.dokliik = array_position((enum_range(NULL :: DOK_VALUUTA)), 'nomenklatuur') and v.dokid = n.id
                where n.id = $1</sql>
    <sqlAsNew>select  $1::integer as id , $2::integer as userid, 'NOMENCLATURE' as doc_type_id,
            null::text as  kood,
            null::integer as rekvid,
            null::text as nimetus,
            null::text as dok,
            null::text as uhik,
            0::numeric as hind,
            0::numeric as ulehind,
            1::numeric as kogus,
            null::text as formula,
            0::integer as status,
            null::text as muud,
            null::text as properties,
            'EUR' as valuuta, 1 as kuurs,
            '20'::text as vat,
            null::text as konto,
            null::text as projekt,
            null::text as tunnus,
            null::text as tegev,
            null::text as allikas,
            null::text as rahavoog,
            null::text as artikkel</sqlAsNew>
    <query />
    <multiple>false</multiple>
    <alias>row</alias>
  </select>
  <selectAsLibs>
    <sql>select * from com_nomenclature 
            where (rekvid = $1 or rekvid is null)
            order by kood</sql>
    <alias>selectAsLibs</alias>
  </selectAsLibs>
  <saveDoc>
    <sql>select libs.sp_salvesta_nomenclature($1, $2, $3) as id</sql>
    <alias>saveDoc</alias>
  </saveDoc>
  <deleteDoc>
    <sql>select error_code, result, error_message from libs.sp_delete_nomenclature($1::integer, $2::integer)</sql>
    <alias>deleteDoc</alias>
  </deleteDoc>
  <requiredFields>
    <validate>kood,nimetus,dok</validate>
  </requiredFields>
  <executeSql>
    <sql></sql>
    <alias></alias>
  </executeSql>
  <executeCommand>
    <sql></sql>
    <alias></alias>
  </executeCommand>
  <register>
    <sql></sql>
    <alias></alias>
  </register>
  <endProcess>
    <sql />
    <alias />
  </endProcess>
  <generateJournal>
    <sql />
    <alias />
  </generateJournal>
</VFPData>
