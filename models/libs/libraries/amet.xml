<?xml version = "1.0" encoding="Windows-1252" standalone="yes"?>
<VFPData>
  <selectAsLibs>
    <sql>select * from com_ametid l
        where  (l.rekvId = $1 or l.rekvid is null)</sql>
    <alias>selectAsLibs</alias>
  </selectAsLibs>
  <select>
    <sql>select l.id, l.rekvid, l.kood, l.nimetus, l.muud, l.status, l.library,
                $2::integer as userid, 'AMET' as doc_type_id,
                (l.properties:: JSONB -&gt;&gt; 'osakondid') :: INTEGER AS osakondId,
                (l.properties:: JSONB -&gt;&gt; 'kogus') :: numeric(18,2) AS kogus,
                (l.properties:: JSONB -&gt;&gt; 'palgamaar') ::integer AS palgamaar,
                (l.properties:: JSONB -&gt;&gt; 'tunnusid') ::integer AS tunnusId
                from libs.library l 
                where l.id = $1</sql>
    <sqlAsNew>select  $1::integer as id , 
            $2::integer as userid, 
            'AMET' as doc_type_id,
            null::text as  kood,
            null::integer as rekvid,
            null::text as nimetus,
            'AMET'::text as library,
            0::integer as status,
            null::integer as osakondId,
            null::numeric(18,2) as kogus,
            null::integer as palgamaar,
            null::integer as tunnusId,
            null::text as muud</sqlAsNew>
    <query />
    <multiple>false</multiple>
    <alias>row</alias>
  </select>
  <saveDoc>
    <sql>select libs.sp_salvesta_amet($1, $2, $3) as id</sql>
    <alias>saveDoc</alias>
  </saveDoc>
  <deleteDoc>
    <sql>select error_code, result, error_message from libs.sp_delete_library($1::integer, $2::integer)</sql>
    <alias>deleteDoc</alias>
  </deleteDoc>
  <grid>
    <sql>select * from cur_ametid a
            where (a.rekvId = $1 or a.rekvid is null)</sql>
    <alias>curAmetid</alias>
  </grid>
  <requiredFields>
    <validate>kood,nimetus,osakondid,library</validate>
  </requiredFields>
</VFPData>
